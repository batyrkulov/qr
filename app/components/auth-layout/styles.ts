import { ImageStyle, StyleProp, ViewStyle } from "react-native"

export const INFO: ViewStyle = {
  alignItems: "center",
  justifyContent: "center",
  paddingVertical: 50,
}

export const LOGO: StyleProp<ImageStyle> = {
  width: 120,
  height: 120,
}

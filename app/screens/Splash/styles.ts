import { ImageStyle, StyleProp, ViewStyle } from "react-native"

export const SCREEN: ViewStyle = { alignItems: "center", justifyContent: "center" }
export const ICON: StyleProp<ImageStyle> = {
  top: -50,
}

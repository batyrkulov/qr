import { TextStyle, ViewStyle } from "react-native"

import { color } from "../../../theme"

export const ERROR_BTN: ViewStyle = {
  borderRadius: 12,
  backgroundColor: color.errorButton,
}

export const ERROR_BTN_TEXT: TextStyle = {
  color: color.error,
  fontSize: 14,
}
